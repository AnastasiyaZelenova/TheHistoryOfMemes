<Window x:Class="UIOfMemes.MemeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UIOfMemes"
        mc:Ignorable="d"
        Title="MemeWindow" Height="600" Width="800" Background="AliceBlue">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="3*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Image Margin="10"></Image>
        <DockPanel Grid.Row="1" Margin="40">
            <TextBlock Text="Year:" FontSize="40"></TextBlock>
            <TextBlock Name="textBlockMemYear" FontSize="50"></TextBlock>
        </DockPanel>
        <StackPanel Orientation="Vertical" Grid.Column="1">
            <TextBlock Text="Description:" FontStyle="Italic" FontSize="35" Margin="10"></TextBlock>
            <TextBlock Name="textBlockDescription" ></TextBlock>
        </StackPanel>
        
    </Grid>
</Window>
